'use strict';

describe('smartHouseApp.mainRoom module', function() {

  beforeEach(module('smartHouseApp.mainRoom'));

  describe('mainRoom controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainRoomCtrl = $controller('MainRoomCtrl');
      expect(mainRoomCtrl).toBeDefined();
    }));

  });
});
