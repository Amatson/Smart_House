'use strict';

describe('smartHouseApp.bedRoom module', function() {

  beforeEach(module('smartHouseApp.bedRoom'));

  describe('bedRoom controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bedRoomCtrl = $controller('BedRoomCtrl');
      expect(bedRoomCtrl).toBeDefined();
    }));

  });
});
